// Handles the line strings for movement tracking of the registrars

//we will use source and layer here as well to trace out the lines
interface FacilitiesLayerProps {
  data: any;
  isLoading: boolean;
}
const RegistrarPathLayer = () => {
  return <div>RegistrarPathLayer</div>;
};

export default RegistrarPathLayer;
